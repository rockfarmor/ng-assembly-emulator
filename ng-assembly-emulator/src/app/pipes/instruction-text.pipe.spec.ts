import { InstructionTextPipe } from './instruction-text.pipe';

describe('InstructionTextPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructionTextPipe();
    expect(pipe).toBeTruthy();
  });
});
